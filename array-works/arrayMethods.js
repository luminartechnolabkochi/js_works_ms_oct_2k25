

/*
var array={
    length,
    push(value), add value at end of array
    pop()Removes the last element from an array and returns it.
    unshift(values) insert new element begining of the array
    shift()Removes the first element from an array and returns it.
    
    //search
    indexOf(value)Returns the index of the first occurrence of a value in an array
    lastIndexOf(value)Returns the index of the last occurrence of a value in an array
    includes(value)Determines whether an array includes a certain element, returning true or false as appropriate.

    //extracting a portion

    slice(start,end)
    splice()

    map()
    filter()
    reduce()
    some()
    forEach()
    find()
    sort()

}

*/

var foods=["dosa","chapathy","friedrice","dosa","coffe","meals"]
//           0        1        2            3      4       5

// foods.splice(1,2)// index from start delete

// console.log(foods);




foods.splice(1,1,"alfham")

console.log(foods);


